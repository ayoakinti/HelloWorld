#include <stdio.h>
#include <cs50.h>
#include <ctype.h>
#include <string.h>

int main(void)
{
    string s = get_string ("Before: ");
    printf("After: ");
    // This line of code is not actually important
    // It can just print all in capitals
    if (islower(s[i]));
    {
        // This code alone would do the job
        printf("%c", toupper(s[i]));
    }
    else
    {
        printf("%c", s[i]);
    }
    printf("\n");
    
}